package com.zstoryproject;import java.io.IOException;import org.lwjgl.LWJGLException;import org.lwjgl.opengl.Display;import org.lwjgl.opengl.DisplayMode;import org.lwjgl.opengl.GL11;import org.newdawn.slick.SlickException;public class StartGame {	private PlayerEntity player;	private PlayerControll playerControll;	public static final int WIDTH = 1280;	public static final int HEIGHT = 800;	private final String TITLE = "Z-Story Project";	public void start() throws SlickException, IOException {		initGL(WIDTH,HEIGHT);		init();				while (true) {			GL11.glClear(GL11.GL_COLOR_BUFFER_BIT);			render();						Display.update();			Display.sync(60);			if (Display.isCloseRequested()) {				Display.destroy();				System.exit(0);			}		}	}		private void initGL(int width, int height) {		try {			Display.setDisplayMode(new DisplayMode(width,height));			Display.setTitle(TITLE);			Display.create();			Display.setVSyncEnabled(true);		} catch (LWJGLException e) {			e.printStackTrace();			System.exit(0);		}		GL11.glEnable(GL11.GL_TEXTURE_2D);    		        		GL11.glClearColor(0.2f, 0.2f, 0.2f, 0.0f);                 	// enable alpha blending        GL11.glEnable(GL11.GL_BLEND);       	GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);               	GL11.glViewport(0,0,width,height);		GL11.glMatrixMode(GL11.GL_MODELVIEW);		GL11.glMatrixMode(GL11.GL_PROJECTION);		GL11.glLoadIdentity();		GL11.glOrtho(0, width, height, 0, 0, 1);		GL11.glMatrixMode(GL11.GL_MODELVIEW);			}		public void init() throws SlickException, IOException {				player = new PlayerEntity();		playerControll = new PlayerControll();	}	public void render() {				GL11.glTexParameteri(GL11.GL_TEXTURE_2D, GL11.GL_TEXTURE_MAG_FILTER, GL11.GL_NEAREST);		player.draw(true, playerControll.animationState());	}	// Main Class	public static void main(String[] argv) throws SlickException, IOException {		StartGame game = new StartGame();		game.start();	}}